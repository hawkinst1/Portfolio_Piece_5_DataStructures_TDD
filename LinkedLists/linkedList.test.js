const {assert} = require("chai");
const linkedList = require("./linkedList");

describe("insertAtHead", () => {
    it("adds an element to the head of the linked list", () => {
        const newlist = new linkedList();

        newlist.insertAtHead(10);
        assert.equal()
    })
})